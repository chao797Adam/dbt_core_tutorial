name: 'dbt_core_proj'
version: '1.0.0'

profile: 'dbt_core_proj'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:         
  - "target"
  - "dbt_packages"

models:
  dbt_core_proj:  
    bronze:
      +schema: bronze      
      +materialized: table  
    
    silver:
      +materialized: table
      +schema: silver        
    
    gold:
      +materialized: table
      +schema: gold          

seeds:
  dbt_core_proj:
    +schema: bronze
    +file_format: delta