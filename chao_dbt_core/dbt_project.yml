name: 'chao_dbt_core'
version: '1.0.0'

profile: 'chao_dbt_core'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:         
  - "target"
  - "dbt_packages"

models:
  chao_dbt_core:  
    bronze:
      +schema: bronze      
      +materialized: table  
    
    silver:
      +materialized: table
      +schema: silver        
    
    gold:
      +materialized: table
      +schema: gold          

seeds:
  chao_dbt_core:
    +schema: bronze
    +file_format: delta  